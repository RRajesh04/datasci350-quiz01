git clone git@github.com:RRajesh04/datasci350-quiz01.git
cd datasci350-quiz01
touch commands.txt
git branch feature-update
git checkout feature-update
mkdir reports
cd reports
touch summary.md
echo "This document provides a summary of the project updates." >> summary.md
pwd 
cd ..
pwd
git add reports
git commit -m "Add reports directory”
touch reports/file{1..3}.txt
mv data/raw-data.csv data/input-data.csv
mkdir scripts/backup && cp scripts/*.py scripts/backup/
rm docs/documentation.md
git add .
git commit -m "Update project files"
touch .gitignore
echo "temp/" >> .gitignore
echo "dataset0?.csv" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file" 
git checkout main
git merge feature-update
